# Shipping Quote Console Application

This is a C# console application that calculates a shipping quote based on user-entered package weight and dimensions. 
The program checks if the package exceeds weight or size limits and then generates a shipping cost using the formula provided.
It includes clear prompts and displays the final quote to the user.
